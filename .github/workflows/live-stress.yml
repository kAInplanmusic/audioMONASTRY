name: Live Stress (on-demand)

on:
  workflow_dispatch:
    inputs:
      base_url:
        description: 'Basis-URL der Live-Instanz (z. B. https://anunnakitools.de)'
        required: true
        default: 'https://anunnakitools.de'

jobs:
  live-stress:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: HTTP/Socket.io-Stresstest
        run: BASE_URL=${{ inputs.base_url }} node scripts/hetzner/stress-test.mjs

      - name: Browser-Engine-Stresstest
        run: BASE_URL=${{ inputs.base_url }} npm run test:stress

      - name: Upload Stress-Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stress-results
          path: test-results/
          retention-days: 7

      - name: Auto-Issue bei Fehler (Erkennung -> Reparatur-Route)
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '⚠️ Live-Stresstest fehlgeschlagen – ' + new Date().toISOString().slice(0, 10),
              body: 'Der Live-Stresstest gegen ${{ inputs.base_url }} ist fehlgeschlagen.\nRun: https://github.com/' + context.repo.owner + '/' + context.repo.repo + '/actions/runs/' + context.runId,
              labels: ['auto-detected', 'bug'],
            });
