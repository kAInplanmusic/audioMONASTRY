[project]
name = "samplemonk-ai-runtime"
version = "1.0.0"
description = "SampleMONK AI Runtime (Hugging Face Custom Container)"
requires-python = ">=3.10"

dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.30,<1",
  "pydantic>=2,<3",
  "torch==2.4.1",
  "transformers>=4.44,<5",
  "soundfile>=0.12",
  "scipy>=1.13",
  "pyannote.audio>=3.3",
]

[tool.setuptools]
packages = []
