# Optional – Qwen3-TTS für voiceMONK (Apache-2.0, inkl. Deutsch)
# ==============================================================
# Nicht im Basis-Lock enthalten, weil qwen-tts transformers exakt auf 4.57.3
# pinnt (Basis-Lock nutzt 4.46.3). Nur aktivieren, wenn voiceMONK den
# Qwen3-TTS-Handler im samplemonk-ai-runtime wirklich nutzen soll:
#
#   docker build --build-arg AI_INSTALL_VOICE_AI=1 -t samplemonk-ai-runtime .
#
# Achtung: Die Installation hebt transformers im Container auf 4.57.3 an.
qwen-tts==0.1.1
transformers==4.57.3
accelerate==1.12.0

# transformers 4.57.3 verlangt huggingface-hub<1 – hier explizit mitpinnen,
# damit der Voice-/Audio-Stack den Pin nicht wieder auf 1.x hebt.
huggingface-hub>=0.34.0,<1.0

# Stem-Separation (biblioMONK #11): demucs ist der aktuell einzige 4-Stem-Pfad
# der Flotte. bs-roformer ist im Handler bewusst als Stub markiert ("Checkpoint/
# audiosep-API muss auf dem Pod verifiziert werden"). Ohne dieses Paket meldet
# der Worker "No module named 'demucs'" und der Stem-Pfad ist tot (live belegt
# 2026-09-12).
demucs
